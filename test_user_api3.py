import urllib.request
import urllib.parse
import json

base_url = "http://localhost:8000"

def get_token():
    # Attempt to login using an existing test user or create one...
    # well maybe we can just query the DB for an existing user and create a token?
    pass
